{
  "name": "rmvcooldown",
  "description": "removes cooldown from a user",
  "options": [
    {
      "name": "user",
      "description": "user to remove cooldown from",
      "type": 6,
      "required": true
    }
  ]
}